# Basic-C-Compiler
A basic C compiler that performs syntax and type checking using lexical analysis. It validates #include, main(), and return, checks balanced brackets, and verifies variable types (int, float, char, bool). It detects errors and generates a report, representing the compiler front-end phase.
